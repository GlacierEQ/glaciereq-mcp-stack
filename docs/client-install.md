# Client Install Notes

Use `configs/mcp.servers.template.json` as the base config for client-specific MCP installs.

## Clients

- Desktop Commander
- Windsurf
- Antigravity
- Codex

## Rules

- Do not store live secrets in config files.
- Resolve secrets from environment variables only.
- Test one server at a time.
